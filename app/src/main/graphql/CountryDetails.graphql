query CountryDetails ($code: ID!){
  country(code: $code) {
      code
      name
      emoji
      phone
      continent {
        name
      }
      capital,
      currency,
      languages{
        code
        name
      }
  }
}